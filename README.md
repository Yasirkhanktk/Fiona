
  # Test

  This is a code bundle for Test. The original project is available at https://www.figma.com/design/d41dBCLia5iO1j0x8yw2QV/Test.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  